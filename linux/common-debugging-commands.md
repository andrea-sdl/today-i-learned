# Common debugging on linux

* `w` users logged, days since reboot, load avg
* `df -h` free space 
* `df -hi` free nodes 
* `netstat -tuple` listening as server, with process id
* `last` last logged users 
* `free -m` free memory